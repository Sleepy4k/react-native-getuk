import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  loader: {
    zIndex: 1,
    width: "100%",
    height: "100%",
    alignSelf: "center",
    alignItems: "center",
    position: "absolute",
    justifyContent: "center",
    backgroundColor: "rgba(255, 255, 255, 0.5)",
  },
});

export default styles;

// Path: src\screens\Loader\styles.js